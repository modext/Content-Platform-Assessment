# Public API origin used by server and browser requests.
NEXT_PUBLIC_API_BASE_URL=https://jsonplaceholder.typicode.com

# Server-only HMAC key. Generate a unique value with at least 32 characters.
AUTH_SECRET=replace-with-a-unique-secret-at-least-32-characters-long
